@using ShopEasyMVC.Helpers
@model ShopEasyMVC.Models.Order

@{
    ViewData["Title"] = "Detalle de pedido";
}

<div class="inventory-page">

    <!-- ENCABEZADO -->
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">

        <div>
            <div class="d-flex align-items-center gap-2 mb-1">
                <h1 class="h3 mb-0 fw-bold page-title">
                    Detalle de Pedido
                </h1>
            </div>

            <p class="mb-0 page-subtitle">
                Información completa de tu pedido.
            </p>
        </div>

        <a asp-action="MyOrders"
           class="btn btn-outline-secondary shadow-sm px-4 rounded-pill fw-semibold">
            Volver
        </a>

    </div>


    <!-- DETALLE -->
    <div class="card enterprise-card mb-4">

        <div class="card-body p-4">

            <div class="d-flex align-items-center gap-2 mb-3">

                <h2 class="h5 fw-bold mb-0 section-title">
                    @Model.OrderNumber
                </h2>

                <span class="badge @Model.Status.ToBadgeClass()">
                    @Model.Status.GetDisplayName()
                </span>

            </div>

            <dl class="row product-detail-dl mb-0">

                <dt class="col-sm-3 enterprise-label mb-2">Total</dt>
                <dd class="col-sm-9 mb-2">@Model.TotalAmount.ToColones()</dd>

                <dt class="col-sm-3 enterprise-label mb-2">Fecha</dt>
                <dd class="col-sm-9 mb-2">@Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</dd>

                <dt class="col-sm-3 enterprise-label mb-2">Dirección de envío</dt>
                <dd class="col-sm-9 mb-2">@Model.ShippingAddress</dd>

                <dt class="col-sm-3 enterprise-label mb-0">Método de pago</dt>
                <dd class="col-sm-9 mb-0">@Model.PaymentMethod.GetDisplayName()</dd>

            </dl>

        </div>

    </div>


    <!-- PRODUCTOS -->
    <div class="card enterprise-card mb-4">

        <div class="card-body p-0">

            <div class="inventory-table-header">

                <div>
                    <h2 class="h6 fw-bold mb-1 section-title">
                        Productos
                    </h2>

                    <p class="small mb-0 section-subtitle">
                        @Model.OrderItems.Count producto(s) en este pedido.
                    </p>
                </div>

            </div>

            <div class="table-responsive">

                <table class="table enterprise-table align-middle mb-0">

                    <thead>

                        <tr>
                            <th class="ps-4">Producto</th>
                            <th>Cantidad</th>
                            <th class="pe-4">Precio unitario</th>
                        </tr>

                    </thead>


                    <tbody>

                        @foreach (var item in Model.OrderItems)
                        {
                            <tr>

                                <td class="ps-4">
                                    <div class="product-name">
                                        @item.Product.Name
                                    </div>
                                </td>

                                <td>
                                    <span class="stock-value">
                                        @item.Quantity
                                    </span>
                                </td>

                                <td class="pe-4">
                                    <span class="product-price">
                                        @item.UnitPrice.ToColones()
                                    </span>
                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>

@section Scripts {

    <style>

        .product-detail-dl dt {
            color: var(--ent-text-muted);
            font-size: 0.82rem;
            font-weight: 600;
        }

        .product-detail-dl dd {
            color: var(--ent-text);
            font-size: 0.9rem;
            font-weight: 600;
        }

    </style>

}